<% range_class = label == 'Créer' ? 'range_start' : 'range_start_edit' %>
<%= simple_form_for([campaign], remote: true) do |f| %>
  <%= f.association :survey, collection: Survey.where(user: current_user) %>
  <%= f.input :title %>
  <%= f.input :start_date, label: "Dates de la campagne", as: :string, required: false, input_html: {id: range_class, class: "d-none"} %>
  <%= f.input :end_date, label: false, as: :string, required: false, input_html: {id: "range_end", class: "d-none"} %>
  <%= f.submit label, class: 'twi-btn lblue-btn' %>
<% end %>
