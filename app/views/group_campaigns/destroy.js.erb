card = document.getElementById(`group-${<%= @group_campaign.group.id %>}`)
card.classList.add('fade-card');


target = document.getElementById()
transfer = (element) => {
  target.insertAdjacentHTML("beforeend", card);
}

transfer;
