<h1>Mes utilisateurs</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.first_name %> <%= user.last_name %>
      <%= user.email %>
    </li>
  <% end %>
</ul>

<div class="twi-form">
  <%= render "form" %>
</div>
