card = document.getElementById(`proposition-${<%= @proposition.id %>}`)
card.classList.add('fade-card');

noDisplay = (element) => {
  card.classList.add('no-diplay');
}

setTimeout(noDisplay, 1000);


